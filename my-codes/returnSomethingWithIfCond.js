if(true && 'Hello')   //this will return Hello.
if(false && 'Hello')  //this will return false.

// this can be used if there is no else part in your code and want to return something based on a condition.

//Ex:
let items = [];
if(items.length === 0 && 'No items found')
